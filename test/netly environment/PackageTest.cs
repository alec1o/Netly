namespace NetlyTest.netly_environment;

public class PackageTest
{
    [Fact]
    public void Start()
    {/*
        {
            var name = "my event name here";
            var body = new byte[] { 0x20, 0x50, 0xCE, 0xFF };
            var package = Package.NewMessage(name, body);
            var bytes = package.SelectMany(x => x).ToList();
            Assert.True(Package.ParseMessage(bytes, out var packageName, out var packageBody));
            Assert.Equal(name, packageName);
            Assert.Equal(bytes, packageBody);
            bytes.Clear();
            packageBody.Clear();
        }
        return;*/
    }
}